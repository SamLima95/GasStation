export interface Cliente {
  id: string;
  nome: string;
  documento: string;
  telefone: string;
  email: string;
  limiteCredito: number;
  saldoDevedor: number;
  unidadeId: string;
  createdAt: string;
}
